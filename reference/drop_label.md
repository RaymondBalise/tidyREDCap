# Drop the label from a variable

There is a reported issues with joins on data (without a reprex) that
seem to be caused by the labels. As a possible solution this can be used
to drop labels.

## Usage

``` r
drop_label(df, x)
```

## Arguments

- df:

  the name of the data frame

- x:

  the quoted name of the variable

## Value

df
